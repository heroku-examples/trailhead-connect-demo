version: '3.1'

services:
  db:
    image: "postgres:alpine"
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
  redis:
    image: "redis:alpine"
    ports:
      - 6379:6379
